name: eventfy_app
description: "A new Flutter project."

environment:
  sdk: ^3.9.0
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # Essenciais para começar
  supabase_flutter: ^2.5.6
  provider: ^6.1.2
  
  # Navegação
  go_router: ^13.2.0
  
  # UI Básica
  google_fonts: ^6.2.1
  
  # Utilidades Básicas
  intl: 0.20.2
  shared_preferences: ^2.2.3
  mask_text_input_formatter: ^2.9.0
  
  # HTTP e JSON
  http: ^1.2.1
  json_annotation: ^4.9.0
  
  # Maps e Localização
  google_maps_flutter: ^2.6.1
  geolocator: ^12.0.0
  geocoding: ^3.0.0
  permission_handler: ^11.3.1
  
  # Upload de imagens
  image_picker: ^1.1.2

  # Exportação e impressão de relatórios
  pdf: ^3.11.0
  printing: ^5.13.1
  path_provider: ^2.1.4
  share_plus: ^10.0.2
  open_filex: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  json_serializable: ^6.8.0
  build_runner: ^2.4.12

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/eventfy_logo.png

# COMENTÁRIO: Depois de ter o app básico funcionando, adicionar gradualmente:
# - image_picker: ^1.1.2 (quando implementar upload)
# - firebase_core + firebase_messaging (quando implementar push notifications)
